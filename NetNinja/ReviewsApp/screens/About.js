import React from "react";
import { StyleSheet, View, Text } from "react-native";

export default function About() {
  return <Text style={styles.conntainer}>About Screen</Text>;
}

const styles = StyleSheet.create({
  conntainer: {
    padding: 24,
  },
});
